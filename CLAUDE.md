# ui-studio 작업 규칙

이 저장소에서 작업할 때 지켜야 하는 규칙. 세션이 끊겨도(메모리가 날아가도) 이 파일은
저장소에 남아있으니, 다음에 누가/어떤 세션이 열어도 여기부터 읽는다.

## 0. 이 저장소와 asset-studio의 관계

`asset-studio/front`가 `"ui-studio": "file:../../ui-studio"`로 이 저장소를 심볼릭 링크
의존성으로 쓴다. 여기서 컴포넌트를 고치면 **`npm run build`(tsup)로 `dist/`를 다시 만들어야**
asset-studio 쪽에 반영된다 — 재설치는 필요 없다.

**ui-studio 작업과 asset-studio 적용은 분리된 별개의 작업이다.** "ui/컴포넌트/패턴/레이아웃을
바꿔줘"라는 요청은 기본적으로 이 저장소만 바꾸라는 뜻이고, asset-studio(소비 프로젝트) 코드를
같이 고치라는 뜻이 아니다. asset-studio까지 적용하고 싶으면 그렇게 명시적으로 요청한다.

## 1. 있는 컴포넌트부터 써라 — raw HTML로 새로 만들지 마라

체크박스, 라디오, 셀렉트, 배지, 버튼처럼 이미 `src/components/`에 있는 걸 `<input
type="checkbox">` 같은 raw HTML로 다시 만들지 않는다. 새 코드(컴포넌트든 Storybook 스토리든)를
쓰기 전에 `src/components/`를 먼저 확인한다.

**정말로 없는데 반복해서 필요한 UI라면**(예: label/value 읽기 전용 행 나열), 그때는 그 자리에서
raw HTML로 때우지 말고 정식 컴포넌트로 만든다. "패턴 스토리 안이니까 괜찮겠지"도 예외 아니다 —
패턴 스토리 자체가 "이 라이브러리의 부품들을 조합하면 이렇게 된다"를 보여주는 게 목적이라, 거기서
raw HTML을 쓰면 그 목적이 깨진다.

## 2. Storybook 카테고리 3단계: 컴포넌트 / 패턴 / 레이아웃

- **컴포넌트** (`컴포넌트/Button` 등): 단일 컴포넌트 하나의 데모.
- **패턴** (`패턴/폼`, `패턴/목록`, `패턴/검색`, `패턴/모달`, `패턴/컨텐츠` 등): **컴포넌트들의
  조합**. 여러 스토리 파일에서 반복해서 쓰는 조합이면(예: 즉시검색/버튼검색 배치) 그 조합을
  `src/stories/SearchBars.tsx`처럼 별도 모듈로 뽑아 `import`해서 재사용한다 — 매번 새로
  구현하지 않는다.
- **레이아웃** (`레이아웃/기본틀`, `레이아웃/상세`, `레이아웃/검색`, `레이아웃/로그인` 등):
  **페이지 단위 구성**. dim 처리된 오버레이(Modal)나 AppShell처럼 화면 전체/한 영역을 통째로
  차지하는 구성이 여기 속한다. 판단 기준: "이게 여러 컴포넌트의 조합일 뿐인가(→패턴)" vs
  "이게 페이지 하나를 구성하는가(→레이아웃)".

새 스토리를 어디 넣을지 애매하면 이 기준으로 판단하고, 애매하면 사용자에게 물어본다(실제로 여러
번 왔다갔다했다 — 상세 모달/페이지가 처음엔 패턴, 나중에 레이아웃으로 재분류됨).

## 3. `레이아웃/기본틀`이 기준값의 출처다 — 새로 정하지 마라

페이지 제목/브레드크럼 스타일을 새로 만들 때 값을 새로 짓지 말고 `src/stories/Layout.stories.tsx`
(`레이아웃/기본틀`)에 있는 값을 그대로 복사한다:

- 브레드크럼: `<div className="mb-4">{breadcrumb}</div>`
- 제목: `<h1 className="mb-2 text-lg font-bold text-[var(--ds-text)]">{title}</h1>`

다른 페이지형 레이아웃(`DetailPageLayout` 등)도 이 값을 그대로 따른다. Modal의 제목(`h2
text-base font-bold`)과는 폰트 크기가 다르다 — 페이지 제목과 모달 제목을 혼동하지 않는다.

## 4. `ContentArea` 안에 또 박스를 두르지 마라

`ContentArea`가 이미 좌상단이 둥근 페이지 컨테이너 역할을 한다. `ContentArea` 안에 들어갈
목적의 컴포넌트(`DetailPageLayout` 등)는 자기 자신의 `border`/`rounded`/`bg-[var(--ds-surface)]`
박스를 두면 안 된다 — 이중 테두리가 생긴다. `border`/`rounded`/오버레이 배경은 **페이지 흐름
밖에 떠 있는 것**(Modal, Popover, DropdownMenu, Tooltip)에만 쓴다.

## 5. 예시 데이터는 도메인 중립으로 — asset-studio 용어 금지

이 저장소는 여러 프로젝트가 공유하는 디자인 시스템이라, 스토리의 예시 데이터에 "자산", "부동산",
"현금성 자산", "아반떼 CN7" 같은 asset-studio 전용 도메인 용어를 쓰지 않는다. 대신 어떤 프로젝트든
쓸 법한 범용 어휘를 쓴다: `홈`, `공지사항`, `사용자`, `설정`, `상세`, `목록` 등. 지금 스토리
전반이 "공지사항/사용자 관리" 같은 가상의 범용 도메인으로 통일돼 있다 — 새 예시를 추가할 때도
이 어휘를 재사용한다.
